# 🧭 Advanced Todo List

> Современное приложение задач, построенное на **React + TypeScript + Zustand + Axios**, демонстрирующее чистую архитектуру, анимированные состояния и удобный UX.
> Приложение использует публичный API [`dummyjson.com`](https://dummyjson.com/todos) для получения и обновления данных.

---

## 🚀 Основные возможности

* ✅ Добавление, удаление и переключение статуса задач
* 🔄 Загрузка данных с сервера (`axios`)
* 🧠 Локальное хранилище с **persist** через `zustand/middleware`
* 🗂️ Фильтры по статусу: `Все`, `Выполненные`, `Активные`
* 💾 Сохранение состояния между перезагрузками
* 🎨 Скелетон-лоадеры, анимации удаления и плавный UX
* ⚡️ Полностью написано на **TypeScript strict**
* 🧩 Архитектура: чистые компоненты + единый store

---

## 📦 Технологии

| Категория   | Используемое решение                                    |
| ----------- | ------------------------------------------------------- |
| UI          | React 19, JSX, CSS                                      |
| State       | Zustand 5 + persist                                     |
| HTTP        | Axios                                                   |
| Build       | Vite 7 + TypeScript 5                                   |
| Quality     | ESLint 9 + React Hooks Plugin                           |
| Data Source | [DummyJSON Todos API](https://dummyjson.com/docs/todos) |

---

## 🛠️ Установка и запуск

```bash
# 1. Клонировать проект
git clone https://github.com/your-username/advanced-todo-list.git
cd advanced-todo-list

# 2. Установить зависимости
npm install

# 3. Запустить dev-сервер
npm run dev

# 4. Открыть в браузере
http://localhost:5173
```

---

## 🧩 Структура проекта

```
src/
├── App.tsx                 # Корневой компонент приложения
├── components/
│   ├── todo-container.tsx  # Главный контейнер
│   ├── header.tsx          # Заголовок с кнопкой обновления
│   ├── add-form.tsx        # Форма добавления задач
│   ├── filters.tsx         # Фильтры списка
│   ├── list.tsx            # Список задач
│   └── footer.tsx          # Подвал с подсчётом задач
├── store/
│   └── use-todo-store.ts   # Zustand-хранилище с CRUD-операциями
├── App.css                 # Стили приложения
└── main.tsx                # Точка входа React
```

---

## ⚙️ Основная логика — Zustand Store

Zustand управляет состоянием задач, фильтрацией, асинхронными запросами и персистентностью в `localStorage`.

```ts
export const useTodoStore = create<ITodoState>()(
  persist(todoStore, {
    name: 'todo-storage',
    storage: createJSONStorage(() => localStorage),
  })
);
```

* **fetchTodos()** — загрузка списка с API
* **addTodo()** — добавление задачи (оптимистичное обновление)
* **toggleTodo()** — изменение статуса с rollback при ошибке
* **removeTodo()** — плавное удаление с анимацией
* **filteredTodos()** — вычисление отфильтрованных задач

---

## 🧮 UI-компоненты

| Компонент | Ответственность                           |
| --------- | ----------------------------------------- |
| `Header`  | Отображает заголовок и кнопку обновления  |
| `AddForm` | Добавляет новую задачу                    |
| `Filters` | Меняет фильтр отображения                 |
| `List`    | Рендерит список задач или скелетон        |
| `Footer`  | Показывает общее и выполненное количество |

---

## 🧑‍💻 Скрипты

| Скрипт            | Назначение                      |
| ----------------- | ------------------------------- |
| `npm run dev`     | Запуск Vite dev-сервера         |
| `npm run build`   | Сборка TypeScript + Vite        |
| `npm run preview` | Предпросмотр собранного проекта |
| `npm run lint`    | Проверка кода ESLint            |

---

## 💡 Полезные детали

* Используется **optimistic UI update** для мгновенного UX при добавлении/удалении.
* API-ошибки безопасно обрабатываются, без поломки UI.
* Состояние (`todos`, `filter`) сохраняется между сессиями.
* Компоненты имеют доступ к store через `useTodoStore`.

---

## 🧩 Пример UI (без стилей)

```
Мои задачи
[Что нужно сделать? ________] [Добавить]
[Все] [Выполненные] [Активные]

☑ Купить молоко
☐ Завершить проект
🗑

2 задачи • 1 выполнено
```

---

## 🧱 Возможные улучшения

* [ ] Добавить кнопку «Очистить выполненные»
* [ ] Темизация (dark/light mode)
* [ ] Offline-режим с IndexedDB
* [ ] Тесты с React Testing Library